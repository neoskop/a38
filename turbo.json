{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "test": { "outputs": [] },
        "lint": { "outputs": [] },
        "lint:staged": { "outputs": [] },
        "lint:changed": { "outputs": [] }
    }
}
