{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": { "outputs": ["lib"] },
        "test": { "outputs": [] },
        "lint": { "outputs": [] },
        "lint:staged": { "outputs": [] },
        "lint:changed": { "outputs": [] }
    }
}
